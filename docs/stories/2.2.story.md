# Story 2.2 — Deploy: Frontend no Vercel + Backend no Railway

**Epic:** 2 — Web Ready
**Story ID:** 2.2
**Status:** Draft
**Priority:** High
**Estimate:** M (1 dia)
**Agent:** @devops
**Depends on:** Story 2.1 (autenticacao), Story 1.1 (config bug)

---

## User Story

Como dono do produto,
Quero o ClassyCut acessivel pela internet com uma URL publica,
Para usar a ferramenta de qualquer lugar sem precisar do meu computador ligado.

---

## Contexto

O ClassyCut precisa de duas implantacoes separadas:
1. **Frontend (React)** → Vercel (gratuito, perfeito para SPA estatica)
2. **Backend (FastAPI + FFmpeg)** → Railway (suporta Docker, FFmpeg disponivel)

O backend precisa de Docker porque o FFmpeg deve estar instalado no container.
O Railway suporta deploy via Docker Compose ou Dockerfile diretamente.

**ATENCAO:** O Vercel NAO pode hospedar o backend porque:
- FFmpeg nao funciona em funcoes serverless
- Operacoes de longa duracao (> 10s) sao abortadas
- Nao ha sistema de arquivos persistente

---

## Acceptance Criteria

### Backend (Railway)
- [ ] AC-1: Backend deployado no Railway via Dockerfile existente
- [ ] AC-2: PostgreSQL provisionado no Railway (nao mais SQLite)
- [ ] AC-3: Variaveis de ambiente configuradas no Railway dashboard
- [ ] AC-4: URL publica do backend disponivel (ex: `classycut-api.railway.app`)
- [ ] AC-5: `/api/health` retorna 200 na URL publica
- [ ] AC-6: Storage de arquivos configurado (Railway volume ou S3/R2)

### Frontend (Vercel)
- [ ] AC-7: Frontend deployado no Vercel apontando para URL do Railway
- [ ] AC-8: Variavel de ambiente `VITE_API_URL` configurada no Vercel
- [ ] AC-9: URL publica do frontend disponivel (ex: `classycut.vercel.app`)
- [ ] AC-10: Login funciona na URL publica
- [ ] AC-11: Upload de video funciona na URL publica

### CORS e Seguranca
- [ ] AC-12: CORS no backend configurado para aceitar dominio do Vercel
- [ ] AC-13: HTTPS em ambos os servicos (Railway e Vercel fornecem automaticamente)

---

## Tasks

### Preparacao do Codigo
- [ ] T-1: Adicionar suporte a PostgreSQL no backend (`psycopg2-binary` ou `asyncpg`)
- [ ] T-2: Modificar `config.py` para usar `DATABASE_URL` do Railway automaticamente
- [ ] T-3: Criar `vercel.json` no frontend com configuracao de SPA (redirect all to index.html)
- [ ] T-4: Modificar `frontend/src/api/client.ts` para usar `VITE_API_URL` em producao

### Deploy Backend (Railway)
- [ ] T-5: Criar conta no Railway (railway.app)
- [ ] T-6: Criar novo projeto no Railway a partir do Dockerfile do backend
- [ ] T-7: Provisionar PostgreSQL no Railway
- [ ] T-8: Configurar variaveis de ambiente no Railway
- [ ] T-9: Executar migracoes Alembic no Railway via CLI ou console
- [ ] T-10: Verificar `/api/health` na URL publica

### Deploy Frontend (Vercel)
- [ ] T-11: Criar conta no Vercel (vercel.com)
- [ ] T-12: Conectar repositorio Git ao Vercel
- [ ] T-13: Configurar `VITE_API_URL` no Vercel apontando para Railway
- [ ] T-14: Configurar comando de build: `npm run build` e diretorio: `dist`
- [ ] T-15: Verificar que o app carrega e funciona na URL do Vercel

---

## Variaveis de Ambiente Necessarias

### Railway (Backend)
```
CLASSYCUT_DATABASE_URL=postgresql://...  (Railway fornece automaticamente)
CLASSYCUT_JWT_SECRET_KEY=<gerar com openssl rand -hex 32>
CLASSYCUT_JWT_EXPIRE_DAYS=7
CLASSYCUT_CORS_ORIGINS=["https://classycut.vercel.app"]
CLASSYCUT_DEBUG=false
CLASSYCUT_UPLOAD_DIR=/data/uploads      (se usando volume Railway)
CLASSYCUT_EXPORT_DIR=/data/exports
CLASSYCUT_TEMP_DIR=/data/temp
```

### Vercel (Frontend)
```
VITE_API_URL=https://classycut-api.railway.app
```

---

## Decisao de Storage

**Opcao A — Volume Railway (mais simples, mais rapido)**
- Railway oferece volumes persistentes
- Os arquivos ficam no disco do container
- Limite: depende do plano Railway

**Opcao B — Cloudflare R2 ou AWS S3 (mais robusto)**
- Arquivos na nuvem, separados do servidor
- Escala ilimitada
- Requer implementacao de upload direto para o storage
- Recomendado para producao com muitos usuarios

**Recomendacao inicial:** Opcao A (volume Railway) para comecar rapidamente.
Migrar para Opcao B quando o uso crescer.

---

## File List

**Novos:**
- `classycut/frontend/vercel.json`

**Modificados:**
- `classycut/backend/pyproject.toml` — adicionar psycopg2-binary
- `classycut/backend/app/config.py` — suporte a DATABASE_URL padrao Railway
- `classycut/frontend/src/api/client.ts` — usar VITE_API_URL
- `classycut/frontend/vite.config.ts` — remover proxy hardcoded em producao

---

## Definition of Done

- [ ] `/api/health` retorna 200 na URL publica do Railway
- [ ] Frontend carrega na URL publica do Vercel
- [ ] Login completo funciona (register → login → ver projetos)
- [ ] Upload de video funciona
- [ ] Story atualizada com checkboxes marcados
