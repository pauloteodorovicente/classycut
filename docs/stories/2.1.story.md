# Story 2.1 — Autenticacao de usuarios (cadastro e login)

**Epic:** 2 — Web Ready
**Story ID:** 2.1
**Status:** Draft
**Priority:** High (blocker para deploy web)
**Estimate:** L (2-3 dias)
**Agent:** @dev
**Depends on:** Story 1.1

---

## User Story

Como usuario do ClassyCut na web,
Quero me cadastrar com email e senha e fazer login,
Para que meus projetos e videos sejam privados e acessiveis apenas por mim.

---

## Contexto

O ClassyCut atualmente nao tem nenhuma autenticacao. Qualquer pessoa que
acesse a URL do servidor consegue ver e editar todos os projetos de todos os usuarios.

Isso e aceitavel para uso local/pessoal, mas e um bloqueio absoluto para deploy web.

Esta story implementa autenticacao JWT simples no backend e o fluxo de login/cadastro
no frontend. Apos esta story, todos os endpoints da API exigirao um token valido.

---

## Acceptance Criteria

### Backend
- [ ] AC-1: Endpoint `POST /api/v1/auth/register` cria novo usuario (email + senha)
- [ ] AC-2: Senhas armazenadas com hash bcrypt (nunca em texto plano)
- [ ] AC-3: Endpoint `POST /api/v1/auth/login` retorna JWT access token
- [ ] AC-4: Token JWT tem expiracao de 7 dias (configuravel via env var)
- [ ] AC-5: Todos os endpoints existentes exigem token valido no header `Authorization: Bearer {token}`
- [ ] AC-6: Projetos e midia sao filtrados por `user_id` — usuarios nao veem dados de outros
- [ ] AC-7: Endpoint `GET /api/v1/auth/me` retorna dados do usuario logado
- [ ] AC-8: Modelo `User` adicionado ao banco com migracao Alembic

### Frontend
- [ ] AC-9: Pagina `/login` com formulario de email e senha
- [ ] AC-10: Pagina `/register` com formulario de cadastro
- [ ] AC-11: Token armazenado no `localStorage` apos login
- [ ] AC-12: Axios interceptor adiciona token automaticamente em todas as requisicoes
- [ ] AC-13: Redirect automatico para `/login` quando token expirado ou ausente
- [ ] AC-14: Botao de logout limpa token e redireciona para `/login`
- [ ] AC-15: Rota `/` (HomePage) protegida — requer login

---

## Tasks

### Backend
- [ ] T-1: Instalar dependencias: `python-jose`, `passlib[bcrypt]`, `python-multipart`
- [ ] T-2: Criar modelo `User` (id, email, hashed_password, created_at)
- [ ] T-3: Gerar migracao Alembic para tabela `users`
- [ ] T-4: Adicionar coluna `user_id` em `Project` com FK para `users`
- [ ] T-5: Gerar migracao Alembic para coluna `user_id`
- [ ] T-6: Criar `app/core/security.py` com funcoes de hash e JWT
- [ ] T-7: Criar `app/api/auth.py` com rotas `/register`, `/login`, `/me`
- [ ] T-8: Criar dependencia `get_current_user` para proteger rotas
- [ ] T-9: Aplicar `get_current_user` em todos os routers existentes
- [ ] T-10: Filtrar queries por `user_id` em projects.py e media.py

### Frontend
- [ ] T-11: Criar `src/pages/LoginPage.tsx`
- [ ] T-12: Criar `src/pages/RegisterPage.tsx`
- [ ] T-13: Criar `src/api/auth.ts` com funcoes login/register/me
- [ ] T-14: Criar `src/stores/authStore.ts` com token e user
- [ ] T-15: Adicionar interceptor Axios para injetar Bearer token
- [ ] T-16: Adicionar interceptor Axios para redirect em 401
- [ ] T-17: Criar componente `ProtectedRoute` para rotas autenticadas
- [ ] T-18: Atualizar `App.tsx` com rotas `/login`, `/register` e protegidas

---

## Notas de Implementacao

### JWT com python-jose
```python
from jose import JWTError, jwt
from passlib.context import CryptContext

pwd_context = CryptContext(schemes=["bcrypt"])
SECRET_KEY = settings.jwt_secret_key  # gerar com: openssl rand -hex 32
ALGORITHM = "HS256"
```

### Dependencia de autenticacao
```python
async def get_current_user(
    token: str = Depends(oauth2_scheme),
    db: Session = Depends(get_db)
) -> User:
    # Valida token, retorna usuario ou levanta 401
```

### Variaveis de ambiente novas
```
CLASSYCUT_JWT_SECRET_KEY=    # obrigatorio em producao
CLASSYCUT_JWT_EXPIRE_DAYS=7  # opcional, padrao 7
```

---

## Seguranca

- Nunca logar senhas ou tokens
- Usar HTTPS em producao (Vercel/Railway ja fornecem)
- Secret key deve ter 32+ bytes aleatorios
- Nao expor stack trace de erros de autenticacao

---

## File List

**Novos:**
- `classycut/backend/app/models/user.py`
- `classycut/backend/app/api/auth.py`
- `classycut/backend/app/core/security.py`
- `classycut/backend/app/alembic/versions/xxx_add_users.py`
- `classycut/backend/app/alembic/versions/xxx_add_user_id_to_projects.py`
- `classycut/frontend/src/pages/LoginPage.tsx`
- `classycut/frontend/src/pages/RegisterPage.tsx`
- `classycut/frontend/src/api/auth.ts`
- `classycut/frontend/src/stores/authStore.ts`
- `classycut/frontend/src/components/shared/ProtectedRoute.tsx`

**Modificados:**
- `classycut/backend/app/main.py` — registrar router auth
- `classycut/backend/app/models/__init__.py` — importar User
- `classycut/backend/app/api/projects.py` — proteger e filtrar por user
- `classycut/backend/app/api/media.py` — proteger
- `classycut/backend/app/config.py` — adicionar JWT settings
- `classycut/backend/.env.example` — documentar JWT_SECRET_KEY
- `classycut/frontend/src/api/client.ts` — interceptors
- `classycut/frontend/src/App.tsx` — rotas protegidas

---

## Definition of Done

- [ ] Todos os ACs implementados e testados
- [ ] Nao e possivel acessar `/` sem estar logado
- [ ] Nao e possivel acessar projetos de outro usuario via API
- [ ] `ruff check .` passa no backend
- [ ] `npm run lint` passa no frontend
- [ ] Story atualizada com checkboxes marcados
