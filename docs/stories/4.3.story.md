# Story 4.3 — Compartilhamento de projeto via link publico

**Epic:** 4 — Expansao de Features
**Story ID:** 4.3
**Status:** Draft
**Priority:** Low
**Estimate:** M (1 dia)
**Agent:** @dev
**Depends on:** Story 2.1 (autenticacao JWT estabelecida)

---

## User Story

Como criador de conteudo usando o ClassyCut,
Quero compartilhar um projeto com um colaborador ou cliente via link publico,
Para que ele possa visualizar os arquivos de midia e os cortes definidos
sem precisar criar uma conta no ClassyCut.

---

## Contexto

Atualmente todos os projetos sao privados e exigem login para acesso.
Nao ha forma de mostrar o trabalho para um cliente ou colaborador
sem compartilhar as credenciais da conta.

Esta story implementa um sistema de links de compartilhamento somente-leitura:
o dono do projeto gera um token unico, qualquer pessoa com o link pode
visualizar (mas nao editar) o projeto.

---

## Acceptance Criteria

- [ ] AC-1: Dono do projeto pode gerar um link publico de compartilhamento
- [ ] AC-2: Link tem formato `/share/{token}` com token UUID aletorio
- [ ] AC-3: Pagina de compartilhamento exibe lista de midias e nome do projeto
- [ ] AC-4: Pagina de compartilhamento e somente leitura (sem botoes de edicao)
- [ ] AC-5: Dono pode revogar o link (gerar novo token invalida o anterior)
- [ ] AC-6: Link funciona sem autenticacao (endpoint publico no backend)
- [ ] AC-7: Pagina de share tem visual simplificado (sem sidebar de ferramentas)

---

## Tasks

### Backend
- [ ] T-1: Adicionar coluna `share_token: str | None` ao model `Project`
- [ ] T-2: Criar migration Alembic para a nova coluna
- [ ] T-3: Endpoint `POST /projects/{id}/share` — gera e salva token UUID, retorna URL
- [ ] T-4: Endpoint `DELETE /projects/{id}/share` — revoga token (set NULL)
- [ ] T-5: Endpoint publico `GET /share/{token}` — retorna dados do projeto (sem auth)

### Frontend
- [ ] T-6: Adicionar botao "Compartilhar" no header do editor (icone Share2)
- [ ] T-7: Modal de compartilhamento com campo de URL copiavel e botao "Revogar"
- [ ] T-8: Nova rota `/share/:token` com componente `SharePage.tsx`
- [ ] T-9: `SharePage.tsx` exibe nome do projeto e lista de arquivos de midia (somente leitura)

---

## Notas de Implementacao

```python
# Model
class Project(Base):
    share_token: Mapped[str | None] = mapped_column(String, unique=True, nullable=True)

# Endpoint publico (sem CurrentUser)
@router.get("/share/{token}", response_model=ProjectShareResponse)
def get_shared_project(token: str, db: Session = Depends(get_db)):
    project = db.query(Project).filter(Project.share_token == token).first()
    if not project:
        raise HTTPException(status_code=404, detail="Link invalido ou expirado")
    return project
```

```typescript
// SharePage.tsx — rota publica
// Nao usa ProtectedRoute
// Busca via GET /api/v1/share/{token}
// Exibe apenas nome do projeto + lista de arquivos
```

---

## File List

**Novos:**
- `frontend/src/pages/SharePage.tsx`

**Modificados:**
- `backend/app/models.py` — coluna share_token
- `backend/app/api/projects.py` — endpoints share/revoke
- `backend/alembic/versions/` — nova migration
- `frontend/src/App.tsx` — rota /share/:token
- `frontend/src/api/projects.ts` — funcoes share/revoke

---

## Definition of Done

- [ ] Link gerado abre o projeto sem login
- [ ] Revogar link invalida o acesso imediatamente
- [ ] Pagina de share nao tem acoes de edicao
- [ ] `npm run lint` passa
- [ ] Story atualizada com checkboxes marcados
