# Story 4.1 — Waveform interativa na timeline

**Epic:** 4 — Expansao de Features
**Story ID:** 4.1
**Status:** Draft
**Priority:** Medium
**Estimate:** M (1 dia)
**Agent:** @dev
**Depends on:** Story 3.1 (timeline interativa estabelecida)

---

## User Story

Como editor de video usando o ClassyCut,
Quero ver a onda de audio do video diretamente na timeline principal,
Para identificar visualmente onde ha fala, musica ou silencio
sem precisar ativar a ferramenta de deteccao automatica.

---

## Contexto

Atualmente a waveform so aparece na timeline quando a ferramenta "Auto-Corte" esta ativa
e apos uma deteccao de silencio. A timeline principal mostra apenas uma lista de arquivos.

A waveform deve ser carregada automaticamente quando o usuario seleciona um video,
e exibida como fundo da timeline em qualquer modo de edicao.

O backend ja tem o endpoint `GET /media/{id}/waveform` que retorna os dados de amplitude.
O canvas de desenho ja existe no componente `Timeline.tsx`.

---

## Acceptance Criteria

- [ ] AC-1: Waveform carrega automaticamente quando uma midia de video/audio e selecionada
- [ ] AC-2: Waveform visivel na timeline em todos os modos (media, corte manual, silencio, export...)
- [ ] AC-3: Waveform se adapta ao nivel de zoom (Ctrl+Scroll)
- [ ] AC-4: Indicador de carregamento enquanto waveform e buscada
- [ ] AC-5: Waveform reseta ao trocar de midia selecionada
- [ ] AC-6: Cor da waveform diferente para audio vs video (ex: azul para video, verde para audio)
- [ ] AC-7: Waveform nao quebra a experiencia quando endpoint falha (graceful fallback)

---

## Tasks

- [ ] T-1: Extrair logica de fetch da waveform do `Timeline.tsx` para um hook `useWaveform(mediaId)`
- [ ] T-2: Remover condicao `showSilenceTimeline` do fetch de waveform — carregar sempre que ha midia
- [ ] T-3: Mostrar waveform como fundo da timeline na visualizacao simples (modo lista de midias)
- [ ] T-4: Adicionar skeleton/loading indicator no canvas durante o fetch
- [ ] T-5: Diferenciar cor de waveform por tipo de midia (video=azul, audio=verde)

---

## Notas de Implementacao

O endpoint ja existe:
```
GET /api/v1/media/{mediaId}/waveform?samples=800
```
Retorna `{ samples: number[] }` com valores normalizados 0-1.

O canvas de desenho ja existe em `Timeline.tsx`. A refatoracao e extrair e generalizar.

---

## File List

**Modificados:**
- `frontend/src/components/timeline/Timeline.tsx` — mostrar waveform sempre
- `frontend/src/hooks/useWaveform.ts` (novo) — hook isolado de fetch de waveform

---

## Definition of Done

- [ ] Waveform visivel na timeline sem precisar ativar nenhuma ferramenta
- [ ] Funciona para video e audio
- [ ] Nao quebra quando a API falha
- [ ] `npm run lint` passa
- [ ] Story atualizada com checkboxes marcados
